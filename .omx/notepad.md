# Open Figure Lab Notepad

## Persistent Context

- Product evolved from Open Design, but the target is scientific figure production.
- MVP should stay narrow: Nature/Science-style multi-panel research figures.
- Trust boundary: agents may adjust visual presentation, but must not silently change source data or scientific values.
- The product-facing spec is `figure.yaml`.
- Start with a local CLI; add web UI only after the spec/render/QA loop works.

## Collaboration Notes

- Codex is acting as leader.
- OpenCode and other agents can join later through bounded lanes listed in `docs/agent-collaboration.md`.
- Use `handoff.md` for end-of-thread or cross-agent handoff.

