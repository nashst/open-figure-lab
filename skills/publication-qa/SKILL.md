# publication-qa

Purpose: verify data integrity, journal compliance, accessibility, visual hierarchy, and export quality.

Required checks:

- figure annotations match source data or recorded computations
- no fabricated demo values appear in final outputs
- typography and line widths fit the selected preset
- SVG/PDF outputs preserve editable text when possible
- color choices are distinguishable and not color-only when categories matter

