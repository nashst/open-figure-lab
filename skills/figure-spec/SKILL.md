# figure-spec

Purpose: translate figure intent into a structured `figure.yaml` contract.

Responsibilities:

- capture scientific claim
- declare panel sequence
- bind each panel to data sources
- define layout
- record journal preset
- separate data fields from visual styling

Agents must not invent scientific values. Missing data should be reported as a blocker in the spec or QA report.

